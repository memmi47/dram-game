# 작업이력 — DRAM 사이클 시뮬레이터

이 문서는 프로젝트의 작업 진행 상황을 시간순으로 기록한다. 설계 의도 자체는 `docs/design-v0.4.md`(최종 설계)와
`docs/handoff-prompt-v0.2.md`(구현 착수 지침)에 있으므로 여기서는 반복하지 않고, **무엇을, 누가, 왜 그렇게
했고, 지금 상태가 어떤지**만 기록한다.

## 기록 규칙

- 모든 항목 제목에 **담당자**를 표기한다: `PM`(기획/최종 결정, 사용자) / `Claude`(Claude Code) /
  `Codex`(Codex) — 담당자가 여럿이면 나열, 협업이면 `PM+Claude` 식으로 표기.
- 역할 분담 자체(누가 무엇을 맡는지)는 `docs/team-plan.md`에 살아있는 문서로 관리하고, 여기서는
  실제로 일어난 일만 시간순으로 기록한다.

---

## 2026-08-22 — v0.2 최초 구현 (담당: Claude) (PR #1, main 병합 완료)

### 배경

저장소는 `README.md` 한 줄짜리 초기 상태였다. `docs/handoff-prompt-v0.2.md`와 `docs/design-v0.4.md` 두
문서를 입력받아 v0.2 스코프(시장 엔진 / 생산성 엔진 / 믹스 엔진 / 경쟁 AI 3사, 레버 6개 이하, 40턴)를
단일 HTML 파일로 처음부터 구현했다.

### 구현 순서

1. **아키텍처 확정**: `runQuarter(state, decisions, rng) → newState` 형태의 순수 시뮬레이션 코어를
   `<script>` 안에 `// ==SIM_START==` / `// ==SIM_END==` 주석으로 감싸고, UI 레이어는 그 위에서
   상태를 그리기만 하도록 분리(핸드오프 문서의 관례 지침 그대로).
2. **`validate.js` 작성**: `index.html`에서 코어 텍스트를 추출해 Node.js에서 `require`하고, 15개
   시드 × 5개 고정 전략(균형형/HBM올인/물량방어/보수적생존/공격증설) × 4개 진영으로 몬테카를로를
   돌려 지배 전략 부재·진영 균형·파산율·노드 전환 타이밍을 콘솔에 리포트하도록 구성.
3. **시장 엔진 1차 구현**: Sufficiency = 산업 총 Saleable Bit ÷ 시뮬레이션된 수요, 매 분기 반응함수
   `ΔlnP = k(국면) + 2.7×(-ln Sufficiency)`를 그대로 적용.

### 발견한 문제 1 — 가격 폭주 (10^80 스케일)

`validate.js` 첫 실행에서 Score가 `1.05e+86` 같은 값으로 나왔다. 원인 분석:

- Sufficiency를 "독립적으로 성장하는 두 계열(수요 vs 시뮬레이션된 공급)의 비율"로 계산했는데, 실측
  반응함수 자체가 Sufficiency ≈ 0.97~1.03 근방에서만 검증된 것이라, 국면이 여러 분기(최대 12분기)
  연속으로 극단값 근처에 머물면 `ΔlnP`가 매 분기 그대로 복리 적용되어 가격이 기하급수적으로 폭주했다.

**수정**: 정권(상승/하락 regime) 시작 시 **그 정권 전체의 누적 로그수익률**을 캘리브레이션된 통계
(하락 중앙값 −53.5%, 상승 중앙값 +23.2%, 꼬리 최대 +141%)에서 직접 샘플링하고, 정권의 분기 수만큼
균등 분배하는 방식으로 재설계했다. 국면상수·반응함수 자체(`priceDelta`)는 그대로 두고, 매 분기 적용된
`ΔlnP`로부터 이분탐색으로 역산해 Sufficiency/국면 배지를 되돌려 계산하므로 3.2절 공식과 화면 표시는
계속 일치한다. 플레이어/AI의 실제 증설·가동률 결정은 작은 폭의 추가 가격 압력(`capacityPressure`,
±수 %)으로만 반영해 인과관계(결정→공급→가격)는 유지하되 정권 목표치를 압도하지 않게 했다.
시대(성숙기/성장둔화기/AI슈퍼사이클)는 정권 방향 확률(`pUp`)과 상승/하락 강도 배율로 반영했다.

`/tmp` 스크래치 스크립트로 파라미터를 여러 번 튜닝하며(정권 지속기간, 강도, 시대별 배율) 40턴 누적
가격 변동이 시대별 ASP YoY 표(성숙기 −25~35%/yr, 성장둔화기 −16%/yr, AI슈퍼사이클 +10.6%/yr)와 방향·
자릿수가 맞는지 확인했다.

### 발견한 문제 2 — 마진 0에 가까운 초기 재무 상수

가격 버그를 고친 뒤에도 균형형 전략 기준 파산율이 92%로 나왔다. 진단 스크립트로 SKH의 분기별
재무제표를 출력해보니, 시작 가격(P=100)에서조차 COGS(272.6)가 매출(254.9)을 넘어 turn 1부터
적자였다. 웨이퍼 원가 상수(`waferCost = 1.05 × waferCostMult`)가 우연히 `grossBit×yield×price`와
거의 같은 값이 되어 마진이 사실상 0이었던 것.

**수정**: `waferCost` 기준값을 1.05 → 0.60으로 낮춰 노드2 기준 총마진 ~40%를 확보했고, SG&A(0.12→0.09),
유지보수 capex(0.18→0.13) 상수도 낮췄다. 이후 균형형 전략 파산율은 성장둔화기 기준 약 40~50%로
내려왔다. 성숙기(가장 가혹한 시대 프리셋)는 여전히 70%대로 높은데, 이는 실측 1990~2000년대 DRAM
커머디티 붕괴기(실제로 다수 업체가 파산·합병된 시기)를 반영한 의도된 난이도로 판단해 남겨두었다.

### 검증

- `node validate.js`: 지배 전략 부재(중앙값 기준 밴드 겹침), 진영 균형 PASS(70% 임계 이하), 파산율이
  시대별로 의미 있게 달라짐(성숙기 > 성장둔화기 > AI슈퍼사이클 순으로 가혹).
- Playwright 헤드리스 브라우저로 시작화면 → 진영/시대 선택 → 40턴 완주 → 종료화면까지 실제 클릭
  시뮬레이션, 콘솔/페이지 에러 0건 확인.
- 두 `<script>` 블록 모두 `new Function()`으로 구문 검사 통과.

### 알려진 가정 (README.md에도 기록됨)

- 경쟁 AI 5개 행동편향의 진영별 초기값: 원본 설계 문서 v0.3 5절 수치를 이번 세션에서 확보하지
  못해(핸드오프 문서가 "대화 히스토리 또는 사용자에게 재확인"하라고 명시했으나 해당 히스토리 없음),
  각 진영의 알려진 실제 성향에 기반해 임의로 설정. 실측 근거 없음 — 추후 사용자 확인 필요.
- 노드별 Gross Bit/Wafer, 웨이퍼 원가 등은 설계 문서 8절이 명시한 대로 방향성만 반영한 추정치.
- CXMT KPI의 "국내고객 매출비중"은 실제 지역 데이터가 없어 티어3(채널) 매출 비중으로 근사.
- DoD #2(노드 전환 고민이 턴 12~20에 발생하는지)는 메커니즘 설계(12분기 리드타임 + 상대적 HBM
  게이트)로만 논증했고, 스크립트 전략의 몬테카를로로는 직접 실측하지 못함 — 실제 사람 플레이테스트
  필요.

### 산출물

- `index.html` — 게임 본체 (커밋 `4e18ac5`)
- `validate.js` — 몬테카를로 검증 스크립트
- PR #1 → main 병합 (커밋 `b5fd44d`)

### 다음에 할 만한 것 (미착수, 우선순위 미정)

- 게임 UI 영어 번역 (사용자 요청 시 진행 예정, 현재는 한국어 유지 결정됨)
- 사람 플레이테스트로 DoD #2(노드 전환 타이밍 긴장) 실측
- 경쟁 AI 행동편향 초기값 근거 데이터 확보/재보정
- 몬디아/WSTS 원본 데이터 기반 노드별 Gross Bit/Wafer 정밀 캘리브레이션 (설계 문서 8절 보류 항목)
- 팹 캔버스 시각화는 원본 `fab-lifecycle-pixel.html` 프로토타입 없이 새로 그렸음 — 원본 파일 확보 시
  시각 언어 일치 여부 재검토

---

## 2026-08-22 — 문서 정리: docs/ 신설, 작업이력 시작 (담당: Claude)

원본 설계 문서 2개(핸드오프 프롬프트, design-v0.4)가 채팅에만 있고 저장소에 없던 것을 `docs/`로
버전 관리 시작. `docs/worklog.md`(이 문서) 신설. 이후 사용자 요청에 따라 담당자 표기 규칙 추가.

---

## 2026-08-22 — 팀 운영 체계 1차안 (담당: PM+Claude)

`docs/team-plan.md` 신설 — 에셋 목록/개발 구조/게임성 전략/업무 분장 1차 초안. 상세 내용은 해당
문서 참고. PM 확인이 필요한 오픈 이슈는 `team-plan.md`의 "PM 확인 필요" 섹션에 정리.

---

## 2026-08-22 — PM 결정 3건 반영 + 소스 분리/빌드 도입 (담당: PM+Claude)

PM이 team-plan.md 7절의 오픈 이슈 중 3건을 결정: (1) Codex는 병행 프로토타입형으로 운영, (2) 타겟
유저는 일반 캐주얼 경영게임 유저(재미 우선, 실측 정확도 후순위), (3) 소스 분리 + 빌드 도입.

(1),(2)는 `team-plan.md`에 반영만 하면 되는 결정이지만, (3)은 실제 구조 변경이 필요해 Claude가 그
자리에서 구현:

- `index.html`(812줄)을 `src/styles.css` / `src/core.js`(SIM_START/END 마커 유지) / `src/ui.js`
  세 파일로 sed 분리 후, `template.html` + `build.js`(외부 npm 의존성 없는 순수 Node 스크립트)로
  재조립하도록 구성. core/ui가 원래부터 `window.DramSimCore` 전역 참조로만 통신하고 있었기 때문에
  실제 모듈 번들러(esbuild 등) 없이 단순 문자열 접합만으로 충분했다.
- `node build.js`로 재생성한 `index.html`이 분리 전 원본과 **byte-for-byte 동일**함을 `diff`로 확인.
- `validate.js`가 빌드된 `index.html`이 아니라 `src/core.js`를 직접 읽도록 변경 — 빌드를 먼저
  돌리지 않아도 검증이 가능해짐(더 견고함).
- Playwright 헤드리스 브라우저로 재빌드된 `index.html`을 40턴 완주 스모크 테스트, 콘솔 에러 0건.
- `README.md`의 "실행/검증/아키텍처" 섹션을 새 워크플로우(`src/` 편집 → `node build.js` → 커밋)에
  맞게 갱신. `index.html`은 계속 커밋되는 산출물로 유지 — 빌드 없이 바로 열어서 플레이 가능해야
  하므로.

### 남은 오픈 이슈 (team-plan.md 7절로 이동)
- 병행 프로토타입 첫 주제가 아직 미정 — Claude/Codex가 각자 무엇부터 시도할지 PM 지정 필요.
- 진영 실명 사용 여부, 원본 팹 픽셀아트 프로토타입 확보 여부는 미해결.

---

## 2026-08-22 — 개발 계획 정-반-합 토론 시작 (담당: PM+Claude, Codex 예정)

PM 지시: Codex가 이미 자체 개발 계획을 만들었으니, Claude도 독립적으로 계획을 만들고 정-반-합
(正-反-合) 방식으로 토론해 합의안을 도출하기로 함.

- `docs/plan-claude.md` 작성 — Codex 안을 아직 보지 못한 상태에서 독립적으로 작성(서로 영향받지
  않은 두 안을 먼저 만드는 게 토론의 전제). 핵심 논지: 엔진 정밀화보다 "결정→결과 인과 가시화"를
  최우선으로 두고, 이후 뉴스 이벤트 다양화 → 긴장감 연출 → 팹 비주얼 리디자인(Codex와 병행 프로토타입
  첫 주제로 제안) 순으로 진행. 외부 이미지/오디오 에셋 도입에는 신중론(단일 HTML 파일 원칙과 충돌
  우려) 제기.
- 부수적으로, 저장소에 남아있던 `claude/stealth-acquirer-strategy-5ou71r` 브랜치를 조사 — dram-game으로
  개명되기 전(2026-06-20) "The Stealth Acquirer"라는 무관한 Next.js 프로젝트의 잔재로 확인. PM이
  삭제 요청했으나 `git push --delete`가 403으로 거부되고 GitHub MCP에도 브랜치 삭제 도구가 없어
  Claude 쪽에서는 삭제 불가 — PM이 GitHub 웹에서 직접 삭제해야 함.

### 다음 단계
- `docs/plan-codex.md` 추가 대기 (Codex 산출물)
- 두 안 대조 토론 → `docs/plan-synthesis.md`로 합의안 정리 예정

---

## 2026-08-22 — 브랜치 전략 확정: `main` 공유 베이스 + 각자 작업 브랜치 (담당: PM+Claude)

Codex가 실제로 자기 브랜치를 만들어 작업을 시작하면서, PM이 브랜치 전략을 확정: `main`은 공유
베이스로 두고 Claude/Codex가 각자 작업방 브랜치에서 병렬로 작업한다. `team-plan.md` 4.2에 기록.

- `docs/team-plan.md`의 오픈 이슈였던 "동시 작업 시 브랜치 전략"을 이걸로 종결.
- Claude는 `claude/dev` 브랜치를 새로 만들어 이후 작업은 여기서 진행하고, PM 검토 후 main에
  머지(또는 PR)하는 흐름으로 전환. 지금까지처럼 `main`에 바로 커밋하는 건 이 시점부터 중단.
- Codex 쪽 브랜치명은 아직 미확인 — 확인되면 `team-plan.md`에 기록.

---

## 2026-08-22 — Codex 감사 검증: P0 결함 5건 확인 (담당: Claude, 감사 원안 Codex)

Codex가 `codex/design-v0.5` 브랜치로 독립안과 v0.2 구현 감사(`design-audit-v0.2-v0.4.md`)를 제출.
해당 문서 §6이 "각 결함 의심 항목을 재현하거나 반박하고 코드 근거를 남긴다"를 Claude에게 요청했으므로,
추상적 토론 대신 전부 실행 검증했다.

### 결과: Codex가 지적한 4건 전부 사실, Claude가 5번째를 추가 발견

- **P0-A** Ramp Valley가 설계상 2분기인데 실제로는 1분기만 적용 — `advancePhysical()`이 카운터를
  설정한 직후 같은 호출에서 감소시킨다. 확인.
- **P0-B** 분기당 3개 결정 제한을 코어가 강제하지 않음 — 6개 레버 동시 제출 시 6개 전부 반영.
  제한은 `src/ui.js`에만 존재. 즉 기존 몬테카를로 밸런스 결과는 실제 플레이 규칙을 대표하지 않는다. 확인.
- **P0-C** HBM 배분 0%로 6분기를 보내도 퀄이 통과 상태로 익음 — 설계 문서 §4.2의
  "컨벤셔널→HBM은 느리다" 비대칭이 무력화돼 있다. 확인.
- **P0-D** `state.initialGap`이 플레이어 진영 기준으로만 계산되는데 전 진영 채점에 공유됨.
  SKH 플레이 시 CXMT의 격차축소 항이 항상 0으로 강제된다. 확인.
- **P0-E (Claude 신규 발견, 가장 심각)** `capacityPressure` 부호 반전. 반응함수상
  공급↑ → Sufficiency↑ → 가격↓ 이어야 하는데, `runQuarter()`가 `dlnP`에 `capacityPressure`를
  **더하고** 있어 공급 급증이 가격을 올린다. 14개 시드 중 9개에서 "공급 100배 증설 쪽 가격이 더 높음".
  부호만 뒤집으면 9/14 → 1/14로 정상화되고, 전략간 평균 격차도 549% → 247%로 개선된다
  (Codex가 지적한 466% WARN의 주원인으로 추정).

부수적으로, "시장과 AI가 RNG 스트림을 공유해 결정만 바꿔도 시장 경로가 통째로 바뀔 것"이라는 가설을
세웠다가 계측으로 기각했다 — 동일 시드에서 rng 호출 횟수와 정권 전개가 완전히 일치했다.
Codex 0순위의 고정 시드 fixture를 예상보다 견고하게 만들 수 있다는 뜻이다.

### 산출물
- `docs/plan-claude-response.md` — 항목별 검증 결과, 두 안 대조, 합의 순서(S0~S6) 제안
- `tools/audit-repro.js` — 5건 전부를 재실행으로 재현하는 스크립트(`node tools/audit-repro.js`).
  `src/core.js`를 그대로 로드하므로 수정 후 회귀 확인에도 쓸 수 있다.

### Claude가 철회/양보한 것
- `plan-claude.md`의 "완성된 룰셋" 전제 철회 — P0 5건이 확인된 이상 유지 불가.
- Codex의 0순위(P0 테스트로 잠그기)를 양 안의 1순위(인과 가시화)보다 앞에 두는 데 동의.
  P0-E를 감안하면 "틀린 인과 위에 인과 UI를 얹는" 순서가 될 뻔했다.
- 첫 병행 프로토타입 주제를 팹 비주얼(Claude 제안) → 인과 카드(Codex 제안)로 양보.
- 에셋 정책도 Codex안(manifest로 관리하며 교체 경로 유지)이 Claude의 단순 신중론보다 낫다고 판단.

### 남은 이견
- D-001(시장 진실 원천의 하이브리드 재설계)은 방향에 동의하되 **순서**를 뒤로 미루자고 제안.
  P0-E 수정만으로 인과 방향은 이미 바로잡히고, 하이브리드는 v0.2 때 가격이 10^80까지 폭주했던
  영역을 다시 건드리는 작업이라 고정 시드 fixture(S2)를 갖춘 뒤 A/B로 판단하는 게 안전하다.

### 자기 비판
`README.md`와 PR #1에 "지배 전략 부재(중앙값 기준)"를 통과로 체크했으나 `validate.js`는 평균 기준으로
WARN을 출력하고 있었다. 서로 다른 지표로 통과를 주장한 부정확한 보고였다.

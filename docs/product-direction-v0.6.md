# 제품 방향 v0.6 — 싱글플레이어 재확정 + 메모리 일반화

> **작업 ID**: `H-002`, `H-006`, `H-007`, `BM-001`, `BM-002`
> **요청·최종 결정**: `PM`
> **작성**: `Claude` (PM 결정 받아쓰기 + 사실 확인)
> **선행 검토**: `Codex` — `docs/benchmark-integration-v0.1.md`
> **상태**: PM 결정 반영 완료. H-002 포함 이번 라운드 결정 전부 확정
> **영향 제한**: 이 라운드는 문서만 변경했다. `src/*`, `validate.js`, `template.html`,
> `index.html` 모두 미변경.

이 문서는 `design-v0.5.md` 이후 제품 방향이 크게 흔들렸다가 정착한 경위와 결론을 담는다.
`design-v0.5.md`를 줄 단위로 개조하지 말라는 Codex 권고(`benchmark-integration-v0.1.md` §6)에
따라 별도 문서로 만들었고, `design-v0.5.md`는 그 시점 설계로 동결한다.

---

## 1. 결정 요약

| ID | 항목 | 결정 |
|---|---|---|
| H-006 | 멀티플레이어 여부 | **싱글플레이어(오토마) 유지.** 멀티/M&A는 별도 후속 프로젝트로 분리 |
| H-007 | 제품 명칭 범위 | **"메모리"로 일반화 — 명칭·프레이밍만.** 제품군 실제 확장(NAND 등)은 보류 |
| BM-001 | 보드게임 BM 반영 범위 | **선택적 통합 승인.** 표현·상태 레이어로만 흡수, 전체 규칙 결합은 반려 |
| BM-002 | 첫 BM 슬라이스 | Codex 제안(Fab Operations Board) **유효**, 착수 가능 |
| H-002 | 1회 플레이 시간 | **30~40분 — 원래 승인값으로 복귀** (§5 참고) |

---

## 2. 멀티플레이어를 검토했다가 철회한 경위

다음 세션이 같은 논의를 처음부터 반복하지 않도록 경위와 근거를 남긴다.

### 2.1 발단

PM이 보드게임 벤치마크 기획안(「실리콘 팹」 — Brass: Birmingham / Kanban EV / Silicon Valley /
Acquire의 메커니즘을 메모리 반도체 테마로 이식)을 가져왔다. 7개 축으로 구성된 안이었다:
네트워크 타일 경제 · 폴리오미노 R&D 퍼즐 · 부서 워커플레이스먼트 · 수율 백빌딩 ·
HBM/TSV 리듬게임 · 강제 M&A · 거시 이벤트 카드.

이 중 M&A와 네트워크 타일 충돌은 **여러 사람이 같은 보드에서 부딪혀야 성립하는 메커니즘**이라,
당시 PM은 "진짜 멀티플레이어로 전환 + H-002(30~40분) 폐기"를 일단 결정했다.

### 2.2 철회 근거

**Codex 측** (`benchmark-integration-v0.1.md`, 원작 공식 규칙서를 직접 확인):

- 원작 플레이타임: Brass: Birmingham 60~120분, Kanban EV 90~120분, Silicon Valley 60~120분.
  7축을 전부 독립 미니게임으로 유지하면 현실적 예상은 **120~180분**.
- "10년 동안 한 회사를 경영하고 회고한다"는 현재 제품 약속과 **강제 M&A가 정면 충돌**한다
  (긴 세션 중 플레이어 탈락 위험 포함).
- 물리적으로 네트워크가 접촉했다는 이유로 기업이 합병되는 것은 산업 현실과도 맞지 않는다.
- PM이 제공한 기획안 자체의 결함도 지적: 수율 로지스틱 함수가 붙여넣기 과정에서 누락되어
  **수치 모델을 재현할 수 없고**, 참고문헌에 비공식 요약·공략이 섞여 있다. → 밸런스 근거로 쓸 수 없고
  "재미 후보 라이브러리"로만 사용해야 한다.

**PM 측**:

> "멀티플레이어는 상대방이 없으면 AI가 프로그램을 짜야 하는데 그건 정말 난이도가 높을 것 같거든"

→ 최종 결정: **싱글플레이어(또는 오토마) 유지, 로컬, 혼자서도 플레이 가능해야 함.**

### 2.3 중요 — 롤백할 코드는 없었다

멀티플레이어는 **논의만 됐고 단 한 줄도 구현되지 않았다.** `src/core.js`는 처음부터 끝까지
"플레이어 1 + AI 3사" 구조 그대로였다. 따라서 이 철회는 코드 롤백이 아니라 **문서상 결정의 정정**이다.

---

## 3. 이미 존재하는 오토마 자산

PM이 우려한 "AI를 새로 짜야 하는 난이도"는 **기존 레버 범위에서는 이미 해결돼 있다.**
향후 "오토마를 처음부터 만들어야 하나?" 하는 혼선을 막기 위해 현재 구현 상태를 명시한다.

`src/core.js`의 `aiDecide(c, s, rng)`가 작동하는 오토마다:

- **5개 행동편향**으로 각 경쟁사 성격이 갈린다 —
  관성(`inertia`) / 점유율집착(`shareObsession`) / 자만(`overconfidence`) /
  손실회피(`lossAversion`) / 정치제약(`politicalConstraint`).
- **관성 기반 간헐 재평가**: `reevalInterval = 1 + round(inertia × 5)`. 관성이 높은 회사는 최대
  6분기까지 기존 정책을 유지하다가 재평가한다. 현금 위기(`cash < capacity × 1.5`) 시에는 관성을
  무시하고 강제 재평가.
- **국면 반응**: 루즈/균형 국면에서 적자면 감산하되, 감산 의지는
  `(1-손실회피) × (1-정치제약×0.7) × (1-점유율집착×0.5)`로 계산된다. 정치제약이 0.6을 넘는
  회사는 적자여도 가동률 75% 아래로 못 내려간다.
- **증설·노드 전환·HBM 배분**을 각자 판단한다. 노드 전환 욕구는
  `리더와의 격차×0.25 + 자만×0.3 + (1-관성)×0.2`.

즉 **40턴 완주 가능한 상대가 이미 3사 존재한다.** 남은 과제는 "오토마를 만드는 것"이 아니라,
새 메커니즘(예: 부서 보드, 공유 슬롯)을 도입할 때 그에 맞춰 **오토마를 확장하는 것**이다.

한계도 함께 기록한다: 진영별 행동편향 초기값은 실측 근거가 없는 추정치다(원본 v0.3 §5 수치를
확보하지 못함 — `README.md`와 `worklog.md`에 기록된 기존 한계).

---

## 4. BM 7축 — 싱글플레이어 기준 재판정

Codex의 판정표(`benchmark-integration-v0.1.md` §3)는 대부분 유효하나, 그중 일부는 **멀티플레이어
가능성을 전제로 "별도 모드 후보"로 열어둔 것**이었다. 싱글플레이어가 확정된 지금 그 항목들의
지위가 달라지므로 재정리한다.

| BM 축 | Codex 판정 | 싱글 확정 후 지위 |
|---|---|---|
| 산업 타일 뒤집기 | 우선 반영 | **유지** — 프로젝트 완료 시 앞면→결과면 전환. 싱글에서도 그대로 유효 |
| 부서별 보드 | 우선 반영 | **유지** — 화면 구획으로만. 워커 선점 규칙은 넣지 않음 |
| 제한된 행동력 | 이미 반영 | **유지** — 분기당 3개 결정(D-003)이 이미 같은 긴장 |
| Develop(구세대 제거) | 표현만 반영 | **유지** — 노드 전환을 구세대→신세대 타일 전환으로 표현 |
| Innovator·2nd Mover | 후속 실험 | **유지** — 경쟁 AI 대비 선착 여부를 회고 배지로. 점수화는 S2 이후 |
| 거시 이벤트 카드 | 표현만 반영 | **유지** — 기존 시대·정권을 카드로 공개. 새 랜덤 보정치는 추가하지 않음 |
| 16비트 픽셀·물리 질감 | 아트 실험 | **유지** — 타일·토큰·카드에 한정. 가독성/색각 검증 후 확장 |
| 수율 백빌딩 | 보류 | **보류 유지** — 현 수율은 결정론적이라 추첨 연출을 붙이면 거짓 인과가 됨 |
| 폴리오미노 R&D | 보류 | **보류 유지** — 단, 붙인다면 노드 전환이 아니라 **제품·패키지 설계**에 (Codex 2차 의견) |
| HBM 리듬게임 | 기본 루프 반려 | **제외 확정** — 손기술이 전략 결과를 덮으면 사후 설명성이 깨짐. 양측 일치 |
| 글로벌 공급망·3자원 | 확장팩 후보 | **범위 밖** — 두 번째 경제 엔진. 싱글에서 병목 경쟁의 의미가 약함 |
| 네트워크 타일 경쟁 | 핵심 후보(멀티 전제) | **범위 밖** — 멀티 전용. 후속 프로젝트로 분리 |
| M&A·네트워크 합병 | 별도 모드 후보 | **범위 밖** — 멀티 전용. 후속 프로젝트로 분리 |
| 지출 기반 턴 순서 | 현재 비적합 | **범위 밖** — 분기 동시 정산 구조에 순서 경쟁이 없음 |

**요약**: 싱글에서 살아남는 것은 **표현·상태·기다림·완료 보상** 계열이고, 사라지는 것은
**플레이어 간 충돌** 계열이다. 이는 Codex가 §1에서 내린 결론("첫 통합 대상은 새로운 경제 규칙이
아니라 현재 규칙을 손으로 만지는 듯 이해하게 만드는 상태 표현")과 일치한다.

---

## 5. H-002(세션 길이) — 30~40분으로 복귀 (확정)

**결정: 원래 승인값 30~40분으로 되돌린다.**

경위를 남긴다. 이번 라운드 중간에 PM이 "코덱스 권장: 3축 집중형(75~90분)"을 선택한 적이 있으나,
**그 질문은 멀티플레이어 전제로 제시된 것**이었다(공유 병목 네트워크 경쟁이 핵심 축에 포함된 상태).
같은 답변 묶음에서 멀티플레이어가 철회됐으므로 그 선택의 전제가 사라졌고, PM이 원래 값으로 복귀를
확정했다.

| 시점 | H-002 상태 |
|---|---|
| 최초 승인 | 30~40분 (싱글 전제) |
| 멀티 전환 논의 중 | 사실상 폐기 |
| 멀티 철회 직후 | 일시 미정 |
| **현재** | **30~40분 (복귀 확정)** |

### 확정에 따라 따라오는 제약

- **40턴 유지 시 턴당 처리 시간 45~60초.** 이 예산이 이후 모든 UI·연출 결정의 상한이 된다.
- **Fab Operations Board(BM-002)**: 정보 밀도를 이 예산에 맞춘다. Codex가 성공 기준으로 제시한
  "분기당 중앙값 60초 이하"(`benchmark-integration-v0.1.md` §5)와 정확히 일치하므로, 그 기준을
  그대로 사용한다.
- **결과 공개 연출**: "읽는 데 오래 걸리는 텍스트"보다 "한눈에 읽히는 카드"를 우선한다
  (`design-v0.5.md` §5의 서술이 이 값 기준이었으므로 그대로 유효).
- **플레이테스트**: `playtest-plan-v0.1.md`의 45분 세션(소개 3분 + 플레이 30~35분 + 회고 7~10분)
  전제가 그대로 성립한다 — 조정 불필요.

### 부수 효과 — design-v0.5.md 동결 표기 일부 무효화

`design-v0.5.md`의 "H-002 = 30~40분" 서술은 이 복귀로 **다시 유효해졌다.** 해당 문서 헤더의
동결 표기에서 H-002 항목만 정정했다(나머지 동결 사유는 그대로 유지).

---

## 6. 메모리 일반화 (H-007)

**범위: 명칭·프레이밍만.** 화면·문서의 "DRAM" 표기를 "메모리"로 바꿔 제품군 확장 여지를 열어두되,
시뮬레이션 구조는 그대로 둔다.

**보류 항목**: NAND 등 실제 제품군 추가. `design-v0.4.md` §7이 "v0.4 이후"로 미뤄둔 항목이며,
시장 엔진이 제품군별로 분리돼야 해서 상당한 재설계가 필요하다. 지금 착수하지 않는다.

**현재 코드 상태** (이번 라운드에서는 **변경하지 않음**):
`src/ui.js` 2곳(시작화면 h1, 플레이화면 h1), `template.html` 1곳(title), `validate.js` 1곳
(콘솔 헤더), `src/core.js` 1곳(주석)에 "DRAM" 표기가 남아 있다. H-002가 확정됐으므로 명칭 교체는
**다음 라운드에서 착수 가능**하다. 저장소 이름(`dram-game`) 변경 여부는 그때 함께 결정한다.

---

## 7. 다음 결정 대기 항목

| ID | 항목 | 대기 사유 |
|---|---|---|
| A-004 | 가상 브랜드명 4종 | Codex 초안(`brand-name-proposals-v0.1.md`) PM 검토 대기 |
| X-005 | `design-v0.5.md` 문서 전체 승인 | 동결본이므로 "동결 상태 확인"으로 갈음 가능 |
| D-001 | 시장 엔진 진실 원천 | S6 fixture A/B 이후 (기존 합의 그대로) |
| D-005 | 시대별 파산율 목표 | S2 기준선 이후 (기존 합의 그대로) |
| — | 저장소 이름 변경 여부 | 코드 명칭 교체와 함께 결정 |

## 8. 착수 가능해진 작업 (H-002 확정으로 잠금 해제)

이 세 가지는 더 이상 결정을 기다리지 않는다. 순서는 의존성 기준이다.

1. **코드 명칭 교체 (H-007)** — `src/ui.js`·`template.html`·`validate.js`·`src/core.js`의 "DRAM"
   표기 5곳을 "메모리"로. 소스 수정 후 `node build.js` 필수.
2. **S1 — P0-A~D 버그 수정** — Ramp Valley 1분기 결함(D-002), 결정 예산 코어 미강제(D-003),
   HBM 퀄 무임승차(D-006), CXMT `initialGap` 오용. 각각 실패 테스트를 먼저 작성한 뒤 수정하고,
   `tools/audit-repro.js`가 `CONFIRMED` → `REFUTED`로 뒤집히는지로 검증한다.
3. **BM-002 — Fab Operations Board 슬라이스** — 턴당 60초 예산 기준. Claude/Codex 병행 시안
   (P-001)으로 진행하고 PM이 비교 채택.

단, A-004(브랜드명)가 미결이면 3번의 경쟁사 표기는 `{{FACTION_CODE_N}}` 플레이스홀더로 남긴다
(Codex가 이미 관련 문서를 그 형태로 정리해둠).
